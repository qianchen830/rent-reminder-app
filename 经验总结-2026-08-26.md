# 收租提醒 App 闪退问题经验总结（2026-08-26）

## 问题概述
APK 安装后闪退，多次排查后修复。

## 排查过程

### 第1层：JPush 插件残留
**症状**：`cordova-plugin-jpush` 已 `npm uninstall` 但 AndroidManifest 里仍有 JPush service/receiver

**根因**：
1. `npm uninstall` 只删了 node_modules，Capacitor 的 `android/capacitor-cordova-android-plugins/` 目录保留了旧插件
2. JPush 的 `AndroidManifest.xml` 缺少 `android:exported="true"`（Android 12+ 强制要求）

**修复**：
```bash
# 删掉插件残留目录，重新 cap sync
rm -rf android/capacitor-cordova-android-plugins/
npx cap sync android

# 手动写 AndroidManifest.xml，所有组件必须加 android:exported="true"
```

### 第2层：Gradle 缓存导致 manifest merger 失败
**症状**：即使手动改了 manifest，`cap sync` 每次都会覆盖，导致 build 失败

**根因**：Capacitor sync 会从 node_modules 重新复制插件配置

**修复**：写 Python patch 脚本，每次 `cap sync` 后自动修复
```python
# 关键组件必须在 sync 后手动 patch：
# - cn.jpush.android.service.PluginOppoPushService
# - cn.jiguang.cordova.push.PushService
# - cn.jiguang.cordova.push.JPushEventReceiver
# - cn.jpush.android.service.PluginVivoMessageReceiver
# - cn.jpush.android.service.PluginHuaweiPlatformsReceiver
# - cn.jpush.android.service.PluginMeizuPlatformsReceiver
```

### 第3层：public/assets 目录旧文件残留
**症状**：Vite build 后 dist 里出现旧的 JS 文件（包含 JPush 代码）

**根因**：Vite 的 `public/` 目录会原样复制到 `dist/`，之前开发的旧文件残留在 `public/assets/` 里

**修复**：
```bash
rm -rf public/assets/
npx vite build
```

### 第4层：登录报错 "Unexpected token '<'"
**症状**：App 打开后登录报错，显示 HTML 而不是 JSON

**根因**：Capacitor config 缺少 `server.url`，App 不知道 API 服务器地址

**修复**：
```json
// capacitor.config.json
{
  "server": {
    "url": "http://1.14.45.222:3310"
  }
}
```

### 第5层：网络不通
**症状**：手机在 4G 下无法连接 API

**根因**：`3002` 端口只监听了 `127.0.0.1`，公网无法直接访问。通过 `proxy.js` 在 `3310` 端口代理

**修复**：确保 capacitor.config.json 的 `server.url` 指向正确的代理端口

## JPush 推送功能恢复步骤

### 1. 安装插件
```bash
npm install cordova-plugin-jpush cordova-plugin-jcore
```

### 2. build.gradle 添加 Maven 依赖（解决编译错误）
```groovy
// android/capacitor-cordova-android-plugins/build.gradle
repositories {
    google()
    mavenCentral()
    maven { url 'https://www.jitpack.io' }
}

dependencies {
    implementation 'cn.jiguang.sdk:jcore:5.5.0'
    implementation 'cn.jiguang.sdk:jpush:3.3.8'
}
```

### 3. AndroidManifest.xml（每次 cap sync 后需重新 patch）
```xml
<!-- 所有 JPush 组件必须添加 android:exported="true" -->
<service android:name="cn.jiguang.cordova.push.PushService"
  android:process=":pushcore" android:exported="true">
```

### 4. main.js 初始化代码
```javascript
function initJPush() {
  if (window.JPush) {
    window.JPush.init()
    window.JPush.getRegistrationID((rid) => {
      if (rid) localStorage.setItem('jpush_rid', rid)
    })
    window.JPush.setAlias({ sequence: 1, alias: 'renter' })
  }
}
document.addEventListener('deviceready', initJPush, false)
```

### 5. 真实 AppKey
需要去极光官网注册获取，替换 `cordova.config.json` 里的 `APP_KEY`

## 项目关键配置

| 项目 | 值 |
|------|-----|
| 包名 | com.rentreminder.app |
| server.url | http://1.14.45.222:3310 |
| 后端API | http://localhost:3002 |
| keystore | android/app/rent-reminder.keystore |
| keyAlias | rentreminder |
| compileSdk | 36 |
| minSdk | 24 |
| targetSdk | 36 |
| Gradle | /home/ubuntu/gradle/gradle-8.14.3 |
| Java | /usr/lib/jvm/java-21-openjdk-amd64 |

## 构建命令
```bash
export ANDROID_HOME=/home/ubuntu/android-sdk
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64

cd /home/ubuntu/rent-reminder-app

rm -rf android/app/build android/capacitor-cordova-android-plugins/build
npx cap sync android

# patch manifest (每次sync后都要跑)
python3 /tmp/fix-jpush-manifest.py

cd android
/home/ubuntu/gradle/gradle-8.14.3/bin/gradle assembleDebug --no-daemon
```

## 教训

1. **Cordova 插件卸载必须手动清理残留目录**：仅 `npm uninstall` 不够
2. **manifest merger 问题必须用脚本自动化**：每次 sync 都会被覆盖
3. **Capacitor 项目改动后必须 clean build**：Gradle 缓存很强
4. **server.url 是关键配置**：离线打包模式下 App 通过它找 API 服务器
